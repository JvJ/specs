(ns specs.core
  (:require [cross-map.core :as cmap
             :refer [cross-map cross-cols cross-rows]]))

;;; LEFTOFF: Make this library or whatever
(defn foo
  "I don't do a whole lot."
  [x]
  (println x "Hello, World!"))
